{
  "name": "prosepal-web",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "bunx serve public -l 3000",
    "build": "bun run validate",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "validate": "node scripts/validate-site.js",
    "check:repo": "bun run lint && bun run validate",
    "harness:local": "bun run check:repo && bun run build",
    "check": "bun run check:repo && bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "serve": "^14.2.6"
  }
}
