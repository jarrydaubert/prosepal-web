# Prosepal Web Backlog (TODO Only)

This backlog contains open work only.
Every row must define a clear, testable Definition of Done.
Execution order is top-to-bottom.

| ID | Priority | TODO | Definition Of Done |
| --- | --- | --- | --- |
| `WEB-P1-12` | P1 | Rotate governance audit token before expiry (May 27, 2026) | `GH_ADMIN_TOKEN` is replaced before **May 27, 2026**, repo variable `GH_ADMIN_TOKEN_EXPIRES_ON` is updated to the new expiry (`YYYY-MM-DD`), `Monthly Governance Audit` passes on `main` after rotation (including `governance-token-expiry` evidence), and the new expiry date + successful run URL/ID are recorded in ops notes/backlog. |
| `WEB-P1-15` | P1 | Make the required QA gate trigger for interaction-test changes | `.github/workflows/seo-quality.yml` includes the interaction test paths/config that affect `bun run check` (at minimum `tests/**` and `playwright.interaction.config.js`) or the interaction suite is split into its own required workflow; a PR that changes only interaction tests/config still produces the expected required status check(s); and the workflow/runbook text is updated to reflect the final trigger model. |
| `WEB-P1-14` | P1 | Replace synthetic freshness dates with editorially truthful publish/update signals | Message-page `datePublished`/`dateModified` values and sitemap `lastmod` are derived from explicit per-page editorial metadata (or file-level verified source policy), not global build-date fallbacks; generator scripts document the date-source contract; validation checks fail when required dates are missing/invalid; sample pages and sitemap entries are verified in evidence after regeneration. |
| `WEB-P1-16` | P1 | Align the website brand system with the Flutter app visual language | Shared web tokens, homepage hero, and core content surfaces (`content`, `blog`, `messages`, and legal/support pages) use the same primary background, surface, coral accent, gold accent, and type hierarchy direction as the Flutter app brand system; remaining legacy plum/cream styling is removed from active UI paths or intentionally documented as an exception; updated screenshots/visual snapshots reflect the new direction; and `bun run check` passes after regeneration. |
| `WEB-P1-17` | P1 | Raise production real-experience score above the warning band | The highest-impact above-the-fold performance bottlenecks on the homepage are reduced enough that production Speed Insights no longer shows a warning-state real experience score for the primary audience window; changes are tied to specific metrics (at minimum LCP/FCP or their current replacement in Vercel reporting), documented with before/after evidence, and `bun run check` passes without regressing analytics, accessibility, or interaction behavior. |
| `WEB-P2-5` | P2 | Bring CodeQL source-of-truth into the repo or remove stale governance assumptions | Either an in-repo CodeQL workflow is added under `.github/workflows/` and documented as part of the required security posture, or CodeQL references are removed/updated across runbooks, audits, and required-check assumptions; `scripts/audit-github-policy-drift.js` matches the real repository policy; and evidence/docs show the final expected security scan configuration. |
| `WEB-P2-6` | P2 | Make CI usage budget audits complete for the full lookback window | `scripts/audit-ci-usage-budget.js` paginates through all workflow runs needed for the configured 30-day window, or explicitly fails/skips when the result set is truncated; the monthly-minute calculation cannot silently undercount when more than 100 runs occur in-window; and the updated audit behavior is covered by script-level tests or documented verification evidence. |
| `WEB-P2-4` | P2 | Run full documentation review with repo owner | All first-party docs (`README.md`, `AGENTS.md`, `docs/**/*.md`, `SECURITY.md`, release notes, and runbooks) are reviewed line-by-line with the owner; outdated or ambiguous claims are updated to match current `prosepal-web` behavior; stack references are explicitly web-only where applicable; commands in docs are spot-validated; and a short review log with date, scope, and decisions is committed under `docs/evidence/`. |
| `WEB-P2-7` | P2 | Reposition Prosepal around hard-to-write moments while keeping broad occasion discovery | Homepage hero, primary CTA copy, and at least one major conversion section lead with emotionally difficult, high-stakes, tone-sensitive writing; broad occasion coverage remains available in navigation/SEO surfaces for discoverability; a short strategy note documents the fact/inference/recommendation split and the positioning sentence the site is adopting; App Store/website positioning language is reviewed for alignment; and `bun run check` passes after the copy/structure changes. |
| `WEB-P2-1` | P2 | Roll out verified social proof and trust strip (blocked pending source quotes) | Homepage includes a trust strip near a primary conversion zone (hero or final CTA) with only verifiable proof points; testimonial/review quotes include source notes in docs; unsourced quantitative copy (including unsupported search-volume claims) is removed or replaced with sourced wording; copy and schema validations pass. |
| `WEB-P3-1` | P3 | Reduce governance-policy drift detection lag | `Monthly Governance Audit` also runs on a shorter review loop for governance-sensitive changes (for example on `main` after workflow/policy-script edits, or at least weekly), the runbook documents the new cadence/trigger, and a successful run on the new trigger path is recorded in evidence or ops notes. |
